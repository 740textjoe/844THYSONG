844.THY.SONG FULL SEO LANDING PAGE PACKAGE
==========================================

This bundle contains the full 20-page SEO list we planned.

Upload each folder to the ROOT of the GitHub website repository.
Each folder contains index.html.

All pages reuse:
  /gift/assets/physical-gift.png

Also included:
  sitemap-seo-pages.xml

Do NOT blindly replace an existing sitemap.xml with this file if your current sitemap already contains other site URLs.
Instead, merge these URLs into the existing sitemap.

After uploading, spot-check:
- one desktop page
- one mobile page
- /gift/ CTA
- physical-gift image
- three or four random URLs in an incognito window
